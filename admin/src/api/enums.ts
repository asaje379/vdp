export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum Frequency {
  DAY = 'DAY',
  WEEK = 'WEEK',
  MONTH = 'MONTH',
}

export enum PaiementStatus {
  DEBT = 'DEBT',
  UNPAID = 'UNPAID',
  PENDING = 'PENDING',
  PAID = 'PAID',
  INACTIVE = 'INACTIVE',
}
